apiVersion: v1
kind: Namespace
metadata:
  name: hubii
---
# Example Secret (in production, use External Secrets Operator or Vault)
# Apply manually: kubectl create secret generic hubii-app-secret \
#   --from-literal=app-env=production -n hubii
#
# This file is for reference only — never commit real secret values.
apiVersion: v1
kind: Secret
metadata:
  name: hubii-app-secret
  namespace: hubii
type: Opaque
stringData:
  app-env: production   # Replace with actual value via CI/CD or Vault